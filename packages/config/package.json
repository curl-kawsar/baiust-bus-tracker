{
  "name": "@bus-tracker/config",
  "version": "1.0.0",
  "private": true,
  "description": "Shared configuration for bus tracker",
  "main": "index.js",
  "scripts": {
    "build": "echo 'Build placeholder for Config package'",
    "lint": "echo 'Lint placeholder for Config package'",
    "clean": "echo 'Clean placeholder for Config package'"
  }
}
